{% include "base.html.twig" %}

{# {% block title %}Dice{% endblock %} #}

{% block content %}

<body>
    {% include 'nav.twig' %}
    <div class="container">
        <h1>{{ title }}</h1>

        <p>The dice is rolled {{ numRolls }} times.</p>

        <ul>
            {% for roll in rolls %}
            <li>{{ roll }}</li>
            {% endfor %}
        </ul>
    </div>

    {% include 'footer.twig' %}
</body>

{% endblock %}